/**
 * Заголовок, которым API Gateway передаёт идентификатор текущего пользователя
 * внутренним сервисам. Gateway — единственная точка проверки JWT (§9.2),
 * downstream-сервисы доверяют заголовку и не дублируют аутентификацию.
 *
 * Значение в нижнем регистре: Node нормализует имена входящих заголовков.
 */
export const USER_ID_HEADER = 'x-user-id';
